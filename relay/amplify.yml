version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm ci
    build:
      commands:
        - npm run typecheck
        - npm test
        - npm run build:aws
  artifacts:
    baseDirectory: dist-aws
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
